<UserControl x:Class="FAControl.Views.ExpedienteContratoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FAControl.Views">

  <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,12,0">
    <StackPanel>

      <!-- ============ ENCABEZADO ============ -->
      <DockPanel Margin="0,0,0,16">
        <Button DockPanel.Dock="Left" Content="← Contratos"
                Style="{DynamicResource Boton.Terciario}" VerticalAlignment="Center"
                Command="{Binding VolverCommand}" />

        <!-- El atajo que se pidió: desde los papeles, ir a ver de quién son -->
        <Button DockPanel.Dock="Right" Content="Ver detalles del cliente"
                Style="{DynamicResource Boton.Secundario}" VerticalAlignment="Center"
                Command="{Binding VerClienteCommand}"
                ToolTip="Abre la ficha completa de este cliente" />

        <StackPanel Margin="12,0,12,0" VerticalAlignment="Center">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Codigo}" Style="{DynamicResource Texto.Titulo2}"
                       FontFamily="{DynamicResource Fuente.Mono}" VerticalAlignment="Center" />
            <TextBlock Text="{Binding ClienteNombre}" Style="{DynamicResource Texto.Titulo2}"
                       Margin="12,0,0,0" VerticalAlignment="Center" />
          </StackPanel>
          <TextBlock Style="{DynamicResource Texto.Caption}">
            <Run Text="{Binding ClienteCedula, Mode=OneWay}" /><Run Text="  ·  " /><Run Text="{Binding ClienteTelefono, Mode=OneWay}" /><Run Text="  ·  " /><Run Text="{Binding ResumenTexto, Mode=OneWay}" />
          </TextBlock>
        </StackPanel>
      </DockPanel>

      <!-- ============ EXPEDIENTE ============
           El mismo control compartido que usan DealControl y Alquileres, con el
           ExpedienteViewModel de ESTE préstamo. Los datos no se cruzan entre
           estancias: cada expediente cuelga de su propio contrato. -->
      <local:ExpedienteClienteView DataContext="{Binding Expediente}" />
    </StackPanel>
  </ScrollViewer>
</UserControl>
